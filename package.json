{
  "name": "save-in",
  "version": "4.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:bundled",
    "bundle": "rolldown -c rolldown.config.mjs",
    "build:bundled": "node scripts/build-bundled.js && node scripts/with-env.js -u WEB_EXT_API_KEY -u WEB_EXT_API_SECRET -- web-ext build --source-dir dist/bundled-pkg --artifacts-dir web-ext-artifacts --overwrite-dest",
    "build:source": "node scripts/build-source-package.js",
    "release:prepare": "node scripts/prepare-release.js",
    "d": "node scripts/dev-firefox.js",
    "d:chrome": "node scripts/dev-chrome.js --watch",
    "e2e": "node scripts/e2e-parallel.js",
    "e2e:serial": "npm run e2e:chrome && npm run e2e:firefox",
    "e2e:chrome": "node scripts/build-bundled.js && node scripts/with-env.js EXT_DIR=dist/bundled-pkg -- vitest run --config vitest.e2e.config.mjs e2e/chrome.e2e.mjs",
    "e2e:firefox": "node scripts/build-bundled.js && node scripts/with-env.js EXT_DIR=dist/bundled-pkg -- vitest run --config vitest.e2e.config.mjs e2e/firefox.e2e.mjs",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "node scripts/build-bundled.js && web-ext lint --source-dir dist/bundled-pkg -i \"src/**\" && oxlint && oxfmt --check",
    "lint:fix": "oxlint --fix && oxfmt",
    "mockserve": "node test/mockserver",
    "review": "node scripts/review-demo.js",
    "reload": "node scripts/reload.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.browser.json && tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/firefox-webext-browser": "^143.0.0",
    "@vitest/coverage-v8": "^4.1.10",
    "jest-webextension-mock": "^4.2.0",
    "jsdom": "^29.1.1",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0",
    "rolldown": "^1.1.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "web-ext": "^10.4.0"
  },
  "engines": {
    "node": ">=24"
  }
}
