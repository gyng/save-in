{
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "prettier": "echo '💈 Listing files to format. Run yarn prettier:write to write.\n' && prettier --list-different src/**/*.js src/*.js",
    "prettier:write": "prettier --write src/**/*.js src/*.js",
    "package": "mkdir -p build && zip -FS -r build/package.zip . -x *.git* -x .* -x yarn.lock -x package.json -x docs/**\\* -x docs/ -x build/ -x build/**\\* -x node_modules/ -x node_modules/**\\*"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^14.1.0",
    "eslint-config-prettier": "^2.6.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^4.0.0",
    "eslint-plugin-prettier": "^2.3.1",
    "eslint-plugin-react": "^6.10.3",
    "prettier": "^1.7.4"
  }
}
