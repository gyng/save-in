// Compatibility export for callers that used the former feature-owned path.
// The contract lives in shared so platform adapters can depend downward.
export * from "../shared/content-fetch-types.ts";
